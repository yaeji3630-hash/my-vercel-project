# 배달앱 프로젝트 보고서

## DB 구조

- `User`: 회원 정보입니다. 이메일, 암호화된 비밀번호, 이름을 저장하고 주문과 연결됩니다.
- `Restaurant`: 식당 정보입니다. 이름, 설명, 이미지, 카테고리를 저장하고 여러 메뉴를 가집니다.
- `MenuItem`: 메뉴 정보입니다. 메뉴명, 가격, 설명, 이미지, 소속 식당을 저장합니다.
- `Order`: 주문 정보입니다. 주문한 사용자, 총액, 주문 상태, 주문 시간을 저장합니다.
- `OrderItem`: 주문 상세 항목입니다. 어떤 메뉴를 몇 개 주문했는지와 주문 당시 가격을 저장합니다.

이렇게 나눈 이유는 식당, 메뉴, 주문, 주문 상세가 각각 반복되는 데이터이기 때문입니다. 주문 하나에는 여러 메뉴가 들어갈 수 있으므로 `Order`와 `OrderItem`을 분리했습니다.

## 구현한 필수 기능

- 회원가입: 이름, 이메일, 비밀번호로 가입하고 비밀번호는 `bcryptjs`로 해시 처리합니다.
- 로그인/로그아웃: 로그인 시 JWT를 쿠키에 저장하고, 로그아웃 시 쿠키를 삭제합니다.
- 식당/메뉴 조회: Prisma로 PostgreSQL에서 식당과 메뉴 데이터를 불러옵니다.
- 장바구니: 메뉴를 담고 수량 변경, 삭제, 총액 계산을 할 수 있습니다.
- 주문하기: 장바구니 항목을 `Order`와 `OrderItem`으로 저장합니다.
- 내 주문 내역: 로그인한 사용자의 주문 목록을 다시 조회합니다.

## 맞닥뜨린 문제와 해결

1. `package.json`과 실제 코드 의존성이 맞지 않았습니다.
   - `@prisma/client`, `@prisma/adapter-pg`, `pg`, `bcryptjs`, `jose`, Tailwind, Prisma CLI를 추가해 Vercel 빌드 기준을 맞췄습니다.

2. Docker 빌드 중 `pnpm-workspace.yaml`에 `packages` 항목이 없어 설치가 실패했습니다.
   - 현재 루트 프로젝트를 workspace package로 등록해 컨테이너에서 `pnpm install`이 가능하게 수정했습니다.

3. 로그인/회원가입 화면의 비밀번호 placeholder 문자열이 깨져 TSX 문법 오류가 발생했습니다.
   - 해당 화면을 다시 정리해 회원가입과 로그인 시연이 끊기지 않도록 수정했습니다.

## 한계

- 결제 기능과 실제 배달 상태 변경 기능은 구현하지 않았습니다.
- 운영 DB migration은 안전을 위해 Vercel 빌드에 자동 포함하지 않고, 배포 전 `pnpm prisma migrate deploy`로 실행하는 방식을 사용합니다.
- 로컬에서는 Docker PostgreSQL을 쓰고, 배포에서는 Neon 같은 외부 PostgreSQL을 사용해야 합니다.
